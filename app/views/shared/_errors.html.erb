  <% if record.errors.any? %>
    <br><br>
    <div id="error_explanation" class="row col-md-12" tabindex="0">
      <h2><%= pluralize(record.errors.count, "error") %> prohibited this field from being saved:</h2>

      <ul>
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
